_: {
  plugins.luasnip = {
    enable = true;
    settings = {
      enable_autosnippets = true;
      # might change this to ctrl+y, if memory serves
      store_selection_keys = "<Tab>";
    };
  };
}
